import Layout from '../layout'
import { SideBarContainer } from './style'

function SideBar() {
  return (
    <SideBarContainer>
      <Layout></Layout>
    </SideBarContainer>
  )
}

export default SideBar
